# Deploy-pipeline dependencies for Railway.
#
# Pinned to the same major+minor versions used locally when the v1.17 model
# bundle was trained. scikit-learn in particular needs the matching minor
# version to load pickles without warnings/failure.
#
# Local versions captured 2026-05-20:
#   sklearn 1.6.1, numpy 2.1.3, pandas 2.2.3, scipy 1.15.3, requests 2.32.3

requests==2.32.3
numpy==2.1.3
pandas==2.2.3
scipy==1.15.3
scikit-learn==1.6.1
